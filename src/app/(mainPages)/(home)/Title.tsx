'use client';

import styled from 'styled-components';

var Title = styled.h1`
	font-size: ${24 / 16}rem;
	font-weight: 600;
	margin-top: calc((24 / 16) * 1rem * -1 - 24px);
`;

export default Title;
