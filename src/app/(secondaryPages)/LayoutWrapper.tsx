'use client';

import styled from 'styled-components';

var LayoutWrapper = styled.div`
	min-height: 100dvh;
	isolation: isolate;
	padding: 20px 0;
	display: grid;
	place-items: center;
`;

export default LayoutWrapper;
