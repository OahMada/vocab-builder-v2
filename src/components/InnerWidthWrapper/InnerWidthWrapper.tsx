'use client';

import styled from 'styled-components';

var InnerWidthWrapper = styled.div`
	width: min(100%, 700px);
	margin-left: auto;
	margin-right: auto;
	height: 100%;
`;

export default InnerWidthWrapper;
